export const environment = {
    production:true,
    apiBaseUrl: "http://localhost:8000"
};

// -------------DEFAULT ENVIRONMENT FILE-------------
// -------------FALLBACK ON DEV--------------------
// -----------SAFETY FILE (ALWAYS WRITE)-----------